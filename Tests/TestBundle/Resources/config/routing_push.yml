push:
    path:       /push
    defaults:   { _controller: TestBundle:Push:index }
    methods:    [ GET ]

push_login:
    path:       /push/login
    defaults:   { _controller: TestBundle:Push:login }
    methods:    [ GET ]
